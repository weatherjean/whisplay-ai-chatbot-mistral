import { LLMTool } from "../type";

// Bastard version: Vision disabled (no providers available)
export const addVisionTools = (tools: LLMTool[]) => {
  console.log("Vision tools: DISABLED (bastard version)");
  // No-op: no vision in bastard version
};
